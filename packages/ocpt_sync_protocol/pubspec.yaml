# SPDX-FileCopyrightText: 2026 Benoit Rolandeau <borlnov.obsessio@gmail.com>
#
# SPDX-License-Identifier: Apache-2.0

name: ocpt_sync_protocol
description: >-
  The domain-blind wire format shared by the app and the sync relay: changeset envelopes,
  snapshot descriptors, sequence numbers and error shapes.
publish_to: none

version: 0.1.0

environment:
  sdk: ^3.11.1

dependencies:
  collection: ^1.19.1
  equatable: ^2.0.7
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
